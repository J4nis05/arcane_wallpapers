# arcane wallpapers

Simple python Script that Takes any Video Files placed in the `videos` directory and shows a random Frame from a Random File.


### Setup

```bash
python3 -m venv .venv
```

```bash
source .venv/bin/activate
```

```bash
pip install -r requirements.txt
```

### Usage

```bash
python main.py
```
